# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
mysql-connector-python==8.2.0

# Config & Validation
python-dotenv==1.0.0
pydantic==2.5.0

# Data processing
pandas==2.1.3

# AI - Groq (lightweight API client, no heavy deps)
httpx==0.27.0
groq==0.9.0

# RAG - Vector DB with built-in ONNX embedder (~50MB, no PyTorch)
# This is all you need when EMBEDDING_BACKEND=onnx (the default)
chromadb --prefer-binary

# ─────────────────────────────────────────────────────────────────
# OPTIONAL: Only uncomment if you set EMBEDDING_BACKEND=sentence_transformers
# WARNING: This pulls in PyTorch (~700MB) and causes Windows path-length
# errors unless you enable long paths in Windows registry first.
# ─────────────────────────────────────────────────────────────────
# sentence-transformers==2.3.1

#pip install chromadb --prefer-binary
#pip install groq fastapi python-dotenv
#uvicorn app:app --reload
#pip install -r requirements.txt --prefer-binary
#http://localhost:5173/
