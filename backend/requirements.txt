# ── Web Framework ──────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0

# ── Database ───────────────────────────────────────────────────
mysql-connector-python==8.2.0

# ── Config & Validation ────────────────────────────────────────
python-dotenv==1.0.0
# Loosened to allow LangChain's required minimum (>=2.7.4) while staying under v3
pydantic>=2.7.4,<3.0.0

# ── Data Processing ────────────────────────────────────────────
pandas==2.1.3
numpy==1.26.2

# ── AI — OpenRouter API Client (OpenAI-compatible) ────────────
# Uses langchain_openai to talk to OpenRouter

# ── LangChain + LangGraph (Multi-Agent System) ────────────────
# Core framework
langchain==1.3.1
langchain-openai>=1.0.0

# LangGraph — agent orchestration (replaces AgentExecutor)
langgraph>=0.2.0

# ── RAG — Vector DB with built-in ONNX embedder (~50MB) ───────
# Works out of the box with EMBEDDING_BACKEND=onnx (default)
chromadb --prefer-binary

# ── HTTP ───────────────────────────────────────────────────────
httpx==0.27.0